"use client";import{useEffect}from"react";import{Button}from"@/components/ui/button";export default function ErrorPage({error,unstable_retry}:{error:Error&{digest?:string};unstable_retry:()=>void}){useEffect(()=>console.error("Protected route failed",error.digest),[error]);return <div className="py-20 text-center"><h2 className="font-display text-3xl">Das war ein Satz daneben.</h2><p className="mt-2 text-sm text-[var(--dialed-text-muted)]">Die Daten konnten gerade nicht geladen werden.</p><Button onClick={unstable_retry} className="mt-6 rounded-full">Erneut versuchen</Button></div>}
