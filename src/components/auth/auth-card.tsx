import Link from "next/link";
import { Coffee } from "lucide-react";
export function AuthCard({title,description,children,footer}:{title:string;description:string;children:React.ReactNode;footer?:React.ReactNode}){return <section className="auth-card w-full max-w-[430px] rounded-[30px] border bg-[var(--dialed-surface)] p-7 shadow-[var(--shadow-md)] sm:p-9"><Link href="/" className="mb-10 inline-flex items-center gap-2 text-sm font-bold uppercase tracking-[.12em]"><span className="grid size-9 place-items-center rounded-full bg-[var(--dialed-espresso)] text-white"><Coffee className="size-4"/></span>Dialed</Link><h1 className="font-display text-4xl font-medium">{title}</h1><p className="mt-2 mb-7 text-sm leading-6 text-[var(--dialed-text-secondary)]">{description}</p>{children}{footer&&<div className="mt-6 text-center text-sm text-[var(--dialed-text-secondary)]">{footer}</div>}</section>}
