import Link from "next/link";
import { Coffee } from "lucide-react";
export function AuthCard({title,description,children,footer}:{title:string;description:string;children:React.ReactNode;footer?:React.ReactNode}){return <section className="auth-card w-full max-w-[430px] border border-black bg-white p-7 sm:p-10"><Link href="/" className="mb-12 inline-flex items-center gap-3"><span className="grid size-10 place-items-center border border-black bg-black text-white"><Coffee className="size-5"/></span><span className="font-display text-xl font-bold">Dialed</span></Link><h1 className="font-display text-[40px] font-bold leading-[1.1]">{title}</h1><p className="mt-4 mb-8 text-sm leading-6 text-[var(--dialed-text-secondary)]">{description}</p>{children}{footer&&<div className="mt-8 border-t pt-5 text-center text-sm text-[var(--dialed-text-secondary)]">{footer}</div>}</section>}
